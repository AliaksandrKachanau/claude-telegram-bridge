# COSTS.md — Расходы: Claude через Telegram vs напрямую

Практический разбор: кто, за что и сколько берёт, и где смотреть реальные цифры.
Цель — понимать, когда и сколько тратится токенов/денег.

## Главное в одном абзаце
- **Цена Claude одинакова** через Telegram и напрямую: бот запускает тот же
  `claude -p` с той же моделью (`glm-5.1`), токены считаются одинаково. Канал связи
  цену Claude не меняет.
- Через Telegram к цене Claude **добавляются** (по желанию): STT (распознавание
  голоса) и TTS (озвучка ответа). Обе величины — копейки или $0.
- **≈99% расходов — это Claude.** STT/TTS на фоне пренебрежимо малы.

## Кто за что берёт деньги

| Компонент | Провайдер (по умолчанию) | Платно? |
|---|---|---|
| Claude (ядро) | `glm-5.1` через `claude -p` | ✅ да — главный расход |
| STT: голос → текст | Groq Whisper-large-v3 | ✅ копейки (есть щедрый free tier) |
| STT: альтернатива | local faster-whisper | ❌ бесплатно, офлайн |
| TTS: текст → голос | Edge TTS | ❌ бесплатно |
| TTS: альтернатива | Silero (локально) | ❌ бесплатно |
| Кодирование аудио | ffmpeg (локально) | ❌ бесплатно |

## Что срабатывает в каждом сценарии

| Сценарий | Claude | STT | TTS |
|---|---|---|---|
| Текст → текст | ✅ | — | — |
| Текст → голос («ответь голосом» или `/voice on`) | ✅ | — | ✅ edge ($0) |
| Голос → текст | ✅ | ✅ groq | — |
| Голос → голос | ✅ | ✅ groq | ✅ edge ($0) |

## Конкретные числа

### Claude (glm-5.1) — наблюдаемая стоимость
Бот уже показывает её в footer каждого ответа: `—$X.XXXX · session … · N turns`.
Это `total_cost_usd` из вывода `claude -p` (считает сам CLI).

- Короткий вопрос/ответ: **≈ $0.02–0.08** (из реальных вызовов владельца: $0.0227, $0.0821).
- Растёт с длиной ответа и, главное, **с контекстом сессии** (см. ниже).
- Точную цену за токен для `glm-5.1` (кастомная модель) снаружи не узнать —
  ориентируйся на `$` из footer.

### STT — Groq Whisper-large-v3
- **$0.111 за час аудио**, минимальная тарификация — **10 сек за запрос**.
- Голосовое 10 сек → 10/3600 × $0.111 ≈ **$0.0003**.
- Голосовое 30 сек → ≈ **$0.0009**.
- 1 минута → ≈ **$0.0019**.
- Free tier Groq: **2000 запросов/день и 7200 сек аудио/час** — для одного
  человека фактически **бесплатно**.
- Альтернативы дешевле: `whisper-large-v3-turbo` — $0.04/час; `stt.provider: local`
  (faster-whisper на этом ПК) — $0.

### TTS
- Edge TTS — **$0** (бесплатный сервис Microsoft).
- Silero локально — **$0**.

## Как растут токены (и стоимость) Claude
Цена ≈ (входные токены × цена_вход) + (выходные × цена_вых) + токены кэша. Что их гонит:

1. **Контекст сессии** — главный драйвер. Бот делает `--resume <session_id>`, поэтому
   каждый следующий ход несёт всю историю диалога. Чем дольше сессия, тем больше
   входных токенов → дороже. `/new` сбрасывает контекст → снова дёшево.
2. **Длина твоего сообщения** (вход).
3. **Длина ответа** (выход) + токены tool-use: если Claude читает файлы, зовёт
   bash/правки — это дополнительные токены.
4. **Режим `/mode`:** `full`/`balanced` могут звать инструменты → больше токенов;
   `strict` и `/ask` (read-only) — обычно меньше.

**Жёсткий лимит:** `--max-budget-usd` (в `config.yaml`, по умолч. `2.0`) — потолок на
один вызов. Через Telegram ты защищён этим капом; при прямом `claude` без флага капа нет.

## Где смотреть реальные цифры
- **Claude $ за вызов:** footer в Telegram; дублируется в `logs/bot.log` (`claude_runner`).
- **Токены (input/output):** сейчас бот их **НЕ показывает** — только `$` и `num_turns`.
  CLI отдаёт поле `usage` в JSON, но бот его не логирует (можно добавить — см. ниже).
- **Groq расход:** <https://console.groq.com/usage>.
- **Итог/агрегат за период:** пока нигде не считается (можно добавить).

## Telegram vs напрямую — есть ли разница в цене Claude?
**Нет.** И там, и там — один и тот же `claude -p`, та же модель `glm-5.1`, те же токены.
Разница не в цене Claude, а в накладных:

| | Через Telegram | Напрямую (терминал) |
|---|---|---|
| Цена Claude за токен | та же | та же |
| + STT при голосовом | +$0.0003–0.002/сообщ. | нет (печатаешь руками) |
| + TTS при озвучке | $0 (edge) | нет |
| Бюджет-кап на вызов | есть (`--max-budget-usd`) | нет, если не передал флаг |
| Сессии | авто-resume → контекст копится | зависит от тебя |

## Как тратить меньше
- `stt.provider: local` → $0 за распознавание (ценой нагрузки на CPU).
- TTS на `edge` (уже по умолчанию) → $0.
- Периодически **`/new`** — сбрасывает контекст, входные токены падают.
- `/ask` для простых вопросов (read-only, меньше tool-use), `/task` — для правок.
- Следи за footer: если `$` за вызов растёт — сессия разрослась, делай `/new`.

## Источники (цены STT)
- Groq Whisper-large-v3: $0.111/час аудио, мин. 10 сек — <https://console.groq.com/docs/model/whisper-large-v3>, <https://groq.com/pricing>
- Whisper-large-v3-turbo: $0.04/час — <https://groq.com/blog/whisper-large-v3-turbo-now-available-on-groq-combining-speed-quality-for-speech-recognition>
- Free tier лимиты (2000 req/день, 7200 сек/час) — <https://www.grizzlypeaksoftware.com/articles/p/groq-api-free-tier-limits-in-2026-what-you-actually-get-uwysd6mb>

> Цены приведены по состоянию на 2026-06. Проверяй актуальные на <https://groq.com/pricing>.
