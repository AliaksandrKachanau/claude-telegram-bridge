# What's New

Короткая выжимка изменений по датам, новое сверху. Полная документация —
[README.md](README.md), технические детали — `git log`.

## 2026-09-17

- **MT5-бот больше не молчит на незнакомые сообщения.** Написать ему команду
  с опечаткой или просто текст мимо кнопок (например, «/help» с лишним
  пробелом впереди) — раньше не отвечало ничего; теперь любая нераспознанная
  команда/сообщение получает короткую подсказку со ссылкой на /help. Заодно
  /help дополнен: про меню снимков и кнопку «🔄 Обновить список» (список
  символов графиков бот узнаёт сам при подключении, опросом «кто жив?»).
  И stop_mt5bot.bat теперь предупреждает, если бот был запущен «от
  администратора»: командная строка такого процесса скрыта, скрипт не может
  его ни найти, ни остановить (закрыть окно вручную или перезагрузить ПК).

## 2026-09-16

- **Индикатор GRE_Shot стал видимым (v1.13).** Индикатор прозрачен по дизайну
  (ничего не рисует на графике), и его работу не было видно. Теперь в правом
  верхнем углу графика, сразу под именем советника (его в этом углу рисует
  сам терминал), живёт маленькая метка
  «GRE_Shot · ЧЧ:ММ:СС»: тикающие секунды означают, что опрос команд идёт, а
  после каждого снимка там же появляется отметка «last: символ время».
  Обновление индикатора на графиках: перезапустите терминал (⏻ из чата —
  выключить и включить) либо снимите старый экземпляр (Ctrl+I) и перетащите
  GRE_Shot заново — переключение таймфрейма новый .ex5 не подхватывает.
- **Питание терминала из Telegram + выбор графика для снимка.** Терминал теперь
  можно выключать и включать прямо из чата: `/mt5off` (с экраном подтверждения —
  покажет, сколько открыто позиций, и напомнит, что до включения советник ими
  не управляет; TP/SL продолжают работать на сервере) и `/mt5on` (запускает
  терминал, монитор подключается сам и присылает резюме с числом сделок за
  простой). Закрытие мягкое, как крестик, с принудительным завершением только
  если терминал не отвечает 15 с. Кнопка ⏻ появилась и в пульте `/mt5`, и в
  нижней постоянной клавиатуре (тап открывает экран статуса терминала —
  выключение всегда через подтверждение ✅).
  Работает за тем же флагом `mt5.allow_trading` и требует заданного
  `mt5.terminal_path`; автоматика бота по-прежнему никогда не запускает
  терминал сама — только явная команда. Кнопка «📸 График» и `/mt5shot` теперь
  открывают **меню выбора графика**: «Все графики» плюс по кнопке на каждый
  символ, которым терминал торговал (`/mt5shot BTCUSD` по-прежнему снимает
  сразу) — причём список символов бот узнаёт сам: при подключении он
  опрашивает графики командой «кто жив?» (каждый GRE_Shot отвечает маленьким
  файлом вместо снимка), а кнопка «🔄 Обновить список» в меню повторяет
  опрос — делать снимок по всем графикам, чтобы увидеть символы, больше
  не нужно. Заодно ужесточена подчистка: файлы снимков, написанные индикатором
  «с опозданием», удаляются и в конце раунда, и после отправки фото — на диске
  ничего не копится; а опоздавший файл сперва «догоняет раунд» — после отбора
  бот делает ещё один контрольный сбор через 1.5 с (на загруженном ПК снимок
  может появиться на диске с задержкой — кэш записи/антивирус, — и раньше
  такое фото молча терялось).
- **Скриншоты графиков терминала в Telegram (фаза 3).** Новая команда
  `/mt5shot` и кнопка «📸 График» (в постоянной клавиатуре и в пульте `/mt5`)
  присылают в чат PNG каждого графика терминала — с дашбордом советника
  (Room, spread, фильтры входа), чего нет в текстовых командах. Работает так:
  в терминал один раз вешается идущий в комплекте индикатор `GRE_Shot`
  (`mt5bot\GRE_Shot.mq5`, по экземпляру на каждый график), бот общается с ним
  через командный файл в общем каталоге терминалов; несколько графиков —
  несколько фото (кроме графиков одного символа — по одному снимку на символ),
  `/mt5shot BTCUSD` — только указанный символ. Повторный тап во время съёмки
  получает «⏳ уже делается» вместо порчи результатов. Снимки
  приходят новыми сообщениями, временные файлы бот подчищает сам. Без
  индикатора на графике бот честно скажет об этом.
- **По второму ревью — фиксы MT5-бота.** Русская форма `/mt5 ордера|позиции`
  (при наборе руками) теперь рендерится тем же путём, что кнопки и пульт
  (лимит 20 блоков + кнопка на каждый элемент): раньше полный список уходил
  одним сообщением без лимита и при десятках ордеров grid-советника молча не
  помещался в лимит Telegram 4096 — ответ просто не приходил. Заодно: при
  остановке бота накопившиеся уведомления получают 5 секунд на доставку
  (раньше терялись молча); сбойный тап по кнопке всегда отвечает «ошибка
  чтения», а не тишиной; кнопка «Закрыть/Удалить» из старого сообщения при
  выключенном `allow_trading` отказывает сразу, не после экрана
  подтверждения; короткий обрыв связи с терминалом отмечается как
  «подключение восстановлено» (с числом сделок за простой), а не как новый
  запуск мониторинга.
- Мелочь по вашей наводке: при повторном тапе по старой кнопке закрытия
  бот теперь пишет «Позиция/Ордер №… уже не существует» вместо безликого
  «Объект».
- **Второй бот: MT5-монитор (фаза 1 — наблюдение).** Новая папка `mt5bot\`:
  отдельный процесс со своим токеном (`MT5_BOT_TOKEN` в `.env` — создайте
  второго бота у @BotFather), Claude-часть не импортирует вообще. Подключается
  к запущенному терминалу bossaFX, следит за ордерами/позициями/историей и
  шлёт уведомления: 📈 выставлен pending, ✅ fill, 🎯/🛑 закрытие (причина
  TP/SL/EXPERT + профит), ⚠️ низкий margin level. Команды: `/mt5status` ·
  `/mt5orders` · `/mt5positions` (полные имена — тап в чате шлёт команду
  целиком; есть меню автодополнения). Управление — **кнопками**: постоянная
  клавиатура внизу чата (📊 Статус · ⏳ Ордера · 📋 Позиции — появляется сама со
  стартовым сообщением, тап — и ответ) и пульт `/mt5` с обновлением на месте.
  Списки позиций/ордеров — блоками с цветными метками (у позиций 🟢/🔴 =
  плюс/минус, у pending 📈/📉 по направлению), каждый элемент отделён пустой
  строкой — на телефоне не сливается. **Тап по позиции/ордеру в списке открывает действия**: закрыть по
  рынку / удалить pending — с экраном подтверждения ✅/❌ (работает при
  `mt5.allow_trading: true`, пока выключено — бот отказывает с объяснением).
  Выставление новых ордеров и скриншоты — фазы 2–3
  (торговля будет за флагом `mt5.allow_trading`, пока `false`).
- **`stop_bot.bat` фильтрует строго `bot.py`** (границы слова): имя
  `mt5_bot.py` содержит подстроку `bot.py`, и старый фильтр убил бы соседний
  бот терминала. Взаимный тест пройден: стоп Claude не трогает MT5 и наоборот.
- **Уведомления MT5 переживают сбой сети** (по ревью): события сделок/margin
  идут через очередь с повторами отправки (5 с → 60 с, ~час попыток) —
  раньше неудачная отправка теряла событие навсегда. Заодно закрыт обход
  защиты «не запускать закрытый терминал» при пустом `terminal_path`, а тап
  торгового действия при выключенном `allow_trading` теперь говорит об этом
  сразу в карточке (без лишних экранов подтверждения).
- **Фикс логирования чужих апдейтов** (общий `security.py`, касается обоих
  ботов): тап постороннего по inline-кнопке без текста сообщения больше не
  даёт TypeError в логе.
- **Смена проекта и `/new` запрещены, пока идёт запрос** (раннер `sdk`). Раньше
  они закрывали живую сессию Claude «под ногами» — идущий запрос обрывался, а
  `/new` ещё и отменялся сам (завершившийся запрос возвращал свой номер сессии).
  Теперь бот сразу отвечает: «⏳ идёт запрос — дождитесь или /cancel».
- **`/cancel` честно про окно старта.** Пока SDK-сессия поднимается (первые
  секунды запроса), прервать ход нельзя — раньше бот врал «Сейчас ничего не
  выполняется»; теперь говорит «запрос ещё поднимается, повторите через
  несколько секунд» (заодно чистит очередь).
- **Переключатель раннера в `/config` больше не меняет его на лету** — только
  записывает в `config.yaml`; применяется рестартом (менять исполнитель Claude
  под работающим ботом ломало очередь сообщений). `/status` показывает раннер,
  на котором процесс реально работает.
- **Кнопки ✅/❌ не зависают «зомби».** Если SDK-сессия падала, пока висел
  запрос прав, кнопки оставались активными навсегда — теперь все висящие
  запросы закрываются как отклонённые.
- **Фикс `stop_bot.bat`:** зачистка осиротевшего CLI раннера `sdk` убивала
  сама себя (эталон поиска совпадал с командной строкой самого скрипта) —
  добавлен фильтр по имени процесса.
- **Диктовка принимает и текст.** В режиме `/note` в файл-журнал пишутся не
  только голосовые, но и набранные с клавиатуры сообщения (голос и текст —
  одинаково, Claude не вызывается; вопросы ему — через `/ask`/`/task`).
  Работает и на паузе Claude.
- **`/help` перегруппирован по действиям:** диалог с Claude · голос · заметки ·
  проекты и git · режимы и управление.

## 2026-09-15

- **Фикс: «Claude не завершил запрос» с дампом HTTP-логов.** Если бот был
  запущен из оболочки с переменной `ANTHROPIC_LOG=debug`, CLI печатал
  отладочные логи в тот же поток, что и JSON-ответ — готовый ответ терялся, а
  вместо него приходил кусок дампа. Теперь бот вычищает эту переменную при
  старте (и у дочернего процесса), разбор вывода переживает посторонний мусор
  в stdout (JSON ищется с конца потока), а при неразборчивом выводе в лог
  пишется хвост — там причина, а не первые 4000 символов шума.

## 2026-09-10

- **Новый раннер: `sdk` (живая сессия Claude).** В `config.yaml` появился
  переключатель `runner: subprocess | sdk` (по умолчанию прежний `subprocess` —
  поведение бота не меняется, пока вы сами не переключите). Режим `sdk` держит
  одну живую сессию Claude на проект: контекст не пересобирается заново на каждое
  сообщение, а ответы приходят быстрее.
- **Кнопки прав ✅/❌ в Telegram.** В режиме `sdk` каждое опасное действие
  Claude (запись файла, команда и т.п.) приходит сообщением с кнопками
  «Разрешить / Отклонить» — вы решаете с телефона. Не ответили за
  `permission_timeout_minutes` (по умолчанию 10 мин) — вызов автоматически
  отклоняется. Уточняющие вопросы Claude тоже приходят кнопками с вариантами.
- **Настоящий `/cancel`.** В режиме `sdk` отмена прерывает ход Claude, не убивая
  сессию: контекст сохраняется, следующий вопрос продолжит тот же диалог.
- **Режим `manual`.** Новый четвёртый режим (`/mode manual`): каждое действие
  Claude — только после вашего тапа ✅. Работает только при `runner: sdk`.
- **Очередь сообщений.** В режиме `sdk`, пока Claude думает (или ждёт вашу
  кнопку), новые сообщения не отклоняются, а встают в очередь (до
  `task_queue_max`, по умолчанию 3) и уходят по одному; `/cancel` чистит
  очередь, `/resume` возобновляет её раздачу.
- `/status` и `/config` показывают раннер, очередь и таймаут кнопок; новые
  настройки правятся там же. `stop_bot.bat` заодно прибирает осиротевший CLI
  раннера `sdk` (ваш интерактивный `claude` не трогает).
- Требования: `claude-agent-sdk` в `requirements.txt` (тянет ~200 МБ — CLI в
  комплекте). Без пакета бот работает как раньше на `subprocess`.

## 2026-08-24

- **`/project` — меню с кнопками.** Без аргументов команда теперь показывает
  inline-кнопки по одной на проект (✅ — активный); тап переключает проект,
  меню перерисовывается на месте. `/project <имя>` и `/project add <путь>`
  работают как раньше.
- **Фикс кнопок в старых меню.** Тап по кнопке в удалённом или старше 48 ч
  сообщении теперь присылает свежее меню вместо тихого «ничего не произошло»
  (касается и тогглов `/voice`, `/confirm`, `/draft`, `/reply_voice`, `/note`).
- **Claude больше не путает, где он.** Headless-сессии теперь знают, что
  работают за Telegram-мостом: ответ в чат — только финальный текст. Исчезают
  «план показан выше» и «я не в Telegram» — всё важное приходит в ответе.

## 2026-07-14

- **Голые тогглы показывают статус + кнопку.** `/voice`, `/confirm`, `/draft`,
  `/reply_voice`, `/note` без аргументов отвечают текущим состоянием и
  кнопкой-переключателем прямо в сообщении.

## 2026-06-25

- **Guard на несколько машин.** Если бот уже запущен на другой машине —
  работающий остаётся, новичок молча уступает (без борьбы за getUpdates).

## 2026-06-24

- Фикс: Markdown-ошибки на динамическом тексте (сообщения с `_`/`*` в путях
  молча не отправлялись) — теперь такой текст шлётся как plain.
- Фикс: сообщение single-instance guard печатается один раз, а не спамом.

## 2026-06-23

- **Большой голосовой/текстовый апдейт:** режимы confirm / draft /
  reply_voice, редактирование `/config` кнопками, пагинация длинных ответов.
- Single-instance guard через named mutex — две копии бота на одной машине
  не конкурируют за апдейты.
- Стартовое сообщение ретраится — «пауза» при автозапуске всегда
  сигнализируется владельцу.
- `stop_bot.bat` терпит процессы, исчезнувшие между сканом и kill.
- `setup.bat` — установка в один клик; запуск предпочитает свежий Python
  через `py` launcher.

## 2026-06-22

- Голосовой ввод/вывод (STT Groq / faster-whisper, TTS Edge / Silero) и
  текстовые UX-улучшения — базовый голосовой функционал бота.

---
*Более ранняя история (setup, базовые команды) — в `git log`.*
