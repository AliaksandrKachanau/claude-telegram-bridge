python-telegram-bot==21.6
pyyaml==6.0.2
python-dotenv==1.0.1
edge-tts==7.2.8
httpx==0.28.1
ruamel.yaml==0.19.1

# Live Claude Agent SDK runner (config.yaml: runner: sdk). ~100 MB wheel —
# the Claude Code CLI is bundled inside (claude_agent_sdk/_bundled/claude.exe).
# Installed lazily at runtime: the bot imports and starts fine without it
# (subprocess runner is the default).
claude-agent-sdk==0.2.152

# Optional — only for offline voice transcription (config.yaml: stt.provider: local).
# Install with: .venv\Scripts\pip install faster-whisper
# faster-whisper

# Second bot: MT5 terminal monitor (mt5bot\). Windows-only; attaches to the
# running MetaTrader 5 terminal via IPC. Pulls in numpy. Not needed on machines
# that run only the Claude bot (mt5_monitor imports it lazily).
MetaTrader5==5.0.6180
